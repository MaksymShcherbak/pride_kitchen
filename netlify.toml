[build]
command = """
rustup default stable &&
rustup target add wasm32-unknown-unknown &&
cargo install --locked dioxus-cli --version 0.7.3 &&
dx build --release
"""
publish = "target/dx/pride_kitchen/release/web/public"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
